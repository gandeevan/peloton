

package peloton.logging;


message LogReplayRequest{
    required int32 len = 1;
    required bytes data = 2;
}

message LogReplayResponse{

}

// -----------------------------------
// SERVICE
// -----------------------------------

service WalReplicator {
    rpc ReplayTransaction (LogReplayRequest) returns (LogReplayResponse);
}
